package com.wedservice.backend.module.tours.entity;

/** Matches DB ENUM values on tour_combo_packages.package_role */
public enum TourComboPackageRole {
    included,
    optional,
    recommended
}
